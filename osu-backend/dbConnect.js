require('dotenv').config(); const { MongoClient } = require('mongodb'); const uri = `mongodb+srv://${process.env.MONGODB_USER}:${process.env.MONGODB_PASSWORD}@osu-db.nh5nspe.mongodb.net/?retryWrites=true&w=majority`; const client = new MongoClient(uri); async function run() { try { await client.connect(); console.log("✅ 成功連接到 MongoDB!"); } catch (error) { console.error("❌ 連線錯誤:", error); } finally { await client.close(); } } run();